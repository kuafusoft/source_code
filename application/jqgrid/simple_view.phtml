<?php
	require_once('toolfactory.php');
	$tool = toolFactory::get('kf');
	$editing = isset($this->editing) ? $this->editing : false;
	$cols = isset($this->cols) ? $this->cols : 1;
	$tool->cf($this->model, $editing, $this->value, $cols, false, array(), $this->new);
?>
